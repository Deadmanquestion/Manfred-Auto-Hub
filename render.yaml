services:
  - type: web
    name: manfred-auto-hub
    runtime: static
    buildCommand: npm install && npm run export:web
    staticPublishPath: mobile-app/dist
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
